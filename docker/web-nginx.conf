server {
    listen 3100;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    # Single page application: unknown paths fall back to the entry document.
    location / {
        try_files $uri $uri/ /index.html;
    }
}
